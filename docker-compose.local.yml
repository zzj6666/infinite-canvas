services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      ADMIN_USERNAME: admin
      ADMIN_PASSWORD: admin123
      DATA_DIR: /data
      STATIC_DIR: /app/web/dist
      PUBLIC_ORIGIN: ${PUBLIC_ORIGIN:-}
    volumes:
      - ./data:/data
